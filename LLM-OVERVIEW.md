# LLM Overview — atlas-voice
*Updated: 2026-05-10 07:35 UTC | Tier: micro | Auto-updated: daily cron*

## What This Is
**Generate AI system prompts from your writing patterns**

## Current State
*Status: 🟢 active from local git history* — 3 commits in the rolling window; last commit 9 minutes ago.

## Key Commands
- `python3 -m pytest  # run tests if configured`
- `python3 -m pip install -e .  # editable install`
- `docker compose up -d  # start compose service from the relevant service directory`
- `git status --short`
- `git log --oneline -5`

## Gotchas
- Keep changes small and verify against local docs before assuming project intent.
